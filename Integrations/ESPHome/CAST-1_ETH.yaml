# TRANSITIONAL — this file exists only so the legacy Ethernet-firmware
# manifest path (firmware-e/) keeps publishing while the fielded fleet
# converges. It builds the canonical unified firmware verbatim, so a device
# still on the old Ethernet firmware OTAs straight into apollo-cast-1 and
# from then on updates from the canonical manifest.
#
# Delete this file (and its entries in .github/workflows/build.yml and
# build-beta.yml) once the fleet has had a release cycle or two to update.

packages:
  unified: !include CAST-1.yaml
